<!DOCTYPE html>
<html>
  <head>
    <title>ConnectStudy</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= favicon_link_tag('favicon.ico') %>
  </head>

  <body>
  <!--  <% if user_signed_in? %>
      <%= render 'shared/header' %>
    <% else %>
      <%= render 'shared/guest_header' %>
    <% end %> -->
    <% if user_signed_in? %>
      
      <input id="menu" type="checkbox" />
      <label for="menu" class="open"><i class="material-icons">menu</i></label>
      <label for="menu" class="back"></label>

      <aside class="z-depth-2">

        <div>
          <ul>
            <li class="logo"><%= link_to 'Connect', root_path %></li>
            <li class="nav-link"><%= link_to 'ホーム', root_path %></li>
            <li class="nav-link"><%= link_to 'プロフィール', edit_user_path(current_user) %></li>
            <li class="nav-link"><%= link_to 'ログアウト', destroy_user_session_path, method: :delete %></li>
          </ul>
        </div>
        <div class="side-footer">
          side footer
        </div>
      </aside> 
    <% end %>
    <main>
      <div class="container">
        <%= render 'shared/flash' %>
        <%= yield %>
        <%= debug(params) if Rails.env.development? %> 
      </div>
    </main>
  </body>
</html>
