(function(){
  // 位置情報をもつ変数を用意
  var pos = new google.maps.LatLng(
   <%= @latlng[0] %>, //latitude
   <%= @latlng[1] %>  //longitude
  );

  var map = new google.maps.Map();
  
  map.setCenter(pos)
  var marker = new google.maps.Marker({
    map: map,
    position: pos
  });
})();