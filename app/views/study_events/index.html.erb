<%= render 'shared/title', title: '勉強会一覧' %>

<%= render 'shared/search', something_url: study_events_url %>
          
<div class="row">
  <% if @study_events.exists? %>
    <%= render @study_events %>
  <% else %>
    <div class="no-message">勉強会は見つかりませんでした</div>
  <% end %>
  <%= paginate @study_events %>
</div>

<%= link_to step1_path, class: 'btn-floating tooltipped btn-large waves-effect waves-light red btn-fixed', data: { position: 'top', tooltip: '勉強会を作る' } do %>
  <i class="material-icons">add</i></a>
<% end %>
