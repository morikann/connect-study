<%= form_with model: current_user.bookmarks.build do |f| %>
  <%= hidden_field_tag :study_event, study_event.id %>
  <%= button_tag class: 'btn-floating waves-effect waves-light', type: 'submit' do %>
    <i class="far fa-bookmark"></i>
  <% end %>
<% end %>