<div class="card-panel profile-card">
  
  <div class="profile-title-wrapper">
    <h1 class="profile-title">プロフィール</h1>
  </div>

  <%= form_with model: @user_profile, id: 'form-include-tag', local: true do |f| %>
    <%= render 'shared/error_messages', instance: @user_profile %>
    <div class="row">
      <div class="profile-avatar-wrapper">
        <%= image_tag 'icon_default.png', class: 'circle-img', id: 'file-preview' %>
      </div>

      <div class="avatar-file">
        <%= f.label :avatar, class: 'avatar-upload' do |file| %>
          画像を変更する
          <%= f.file_field :avatar, accept: "image/jpeg,image/gif,image/png", id: 'profile_avatar', style: "display: none" %>
        <% end %>
      </div> 

      <div class="input-field col s12">
        <i class="material-icons prefix"><span class="essential">*</span>account_circle</i>
        <%= f.text_field :username, autocomplete: 'name' %>
        <%= f.label :username %>
      </div>

      <div class="input-field col s12">
        <i class="material-icons prefix"><span class="essential">*</span>today</i>
        <%= f.date_select :birth_date, start_year: 1950, end_year: (Time.now.year - 10), default: Date.new(1989, 1, 1) %>
        <%= f.label :birth_date %>
      </div>

      <div class="input-field col s12 prefecture-wrapper">
        <i class="material-icons prefix"><span class="essential">*</span>place</i>
        <%= f.collection_select :prefecture_id, Prefecture.all, :id, :name, include_blank: "お住まいの県" %>
      </div>

      <div class="input-field col s12">
        <i class="material-icons prefix">mode_edit</i>
        <%= f.text_area :self_introduction, class: "materialize-textarea", style: "height: 200px" %>
        <%= f.label :self_introduction, class: "active" %>
      </div>

      <div class="tag-label">勉強ジャンルタグ</div>
      <div class="input-field col s12">
        <i class="material-icons prefix"><span class="essential">*</span>local_offer</i>
        <div class="chips chips-initial" id="tags-wrapper" style="margin-top: 0px;">
          <%= f.text_field :tag, style: "border-bottom: none;", id: 'tag-field' %>
        </div>
      </div>

      <div class="input-field col s12">
        <i class="material-icons prefix"><span class="essential">*</span>all_out</i>
        <%= f.text_field :purpose, class: "materialize-textarea", style: "height: 100px" %>
        <%= f.label :purpose, class: "active" %>
      </div>

      <div class="center-align">
        <button class="btn waves-effect waves-light">
          <%= f.submit 'プロフィール登録', class: 'submit-btn' %>
          <i class="material-icons right">send</i>
        </button>
      </div>

    </div> 
  <% end %>
</div>

