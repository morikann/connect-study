<% if resource.errors.any? %>
  <!--<div class="teal lighten-4">
    <p>
      <%= I18n.t("errors.messages.not_saved", ount: resource.errors.count, resource: resource.class.model_name.human.downcase)%>
    </p>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li class="red lighten-4 error_message"><%= message %></li>
      <% end %>
    </ul>
  </div> -->
  <% resource.errors.full_messages.each do |message| %>
    <div class="card-panel red lighten-4 flash_message">
      <%= message %>
    </div>
  <% end %>
<% end %>
