<%= form_with(model: current_user.active_relationships.build, reomote: true) do |f| %>
  <%= hidden_field_tag :follower_id, @user.id %>
  <%= f.submit 'フォローする', class: 'waves-effect waves-light btn following-btn' %>
<% end %>