<%= form_with(model: current_user.active_relationships.find_by(follower_id: @user.id), method: :delete, local: true) do |f| %>
  <%= f.submit 'フォローを外す', class: 'waves-effect waves-light btn' %>
<% end %>