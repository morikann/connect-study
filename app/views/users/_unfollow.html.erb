<%= form_with(model: current_user.active_relationships.find_by(follower_id: @user.id), method: :delete, remote: true) do |f| %>
  <%= f.submit 'フォローを外す', class: 'waves-effect waves-light btn unfollow-btn' %>
<% end %>